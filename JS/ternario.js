// TERNARY 

/**
 * 
 * Sintassi: 
 * 
 * condizione ? codiceSeVera : codiceSeFalsa;
 * 
 */

// Esempio 1 

let numero = 6; 
let ternario = (numero % 2 === 0) ? "pari" : "dispari";
console.log(ternario);


let età = 12; 
let accesso = (età < 15) ? "bimbo" : (età < 20) ? "adolescente" : "adulto";
console.log(accesso);



